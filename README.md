# STS2 model training

## Root setup

Install the repository from the project root to make the local CLI tools available in one environment:

```bash
poetry install --no-root
```

After that, you can run the commands from the repository root:

```bash
poetry run sts2-download --help
poetry run sts2-card-pick --help
```
