[project]
name = "gbm-model"
version = "0.1.0"
description = ""
authors = [
    {name = "James Wild",email = "wild.james343@gmail.com"}
]
requires-python = ">=3.12"
dependencies = [
    "lightgbm (>=4.7.0,<5.0.0)",
    "pathlib (>=1.0.1,<2.0.0)",
    "logit-model",
    "sts2-utils",
]

[tool.poetry]
packages = [{ include = "gbm_model", from = "src" }]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
